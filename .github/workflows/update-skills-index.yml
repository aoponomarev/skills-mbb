name: Update Skills Index

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'skills-mbb/skills/**'
      - '!skills-mbb/skills/index/**' # Игнорируем изменения в самом индексе

jobs:
  update-index:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Generate skills index
        run: python skills-mbb/generate_skills_index.py
        working-directory: ${{ github.workspace }}

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add skills-mbb/skills/index/index-mbb.md
          git diff --staged --quiet || git commit -m "Auto: Update skills index"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
