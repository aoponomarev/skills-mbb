---
id: integrations-llm-providers-config
title: "Подключение LLM провайдеров к Continue"
description_ru: "Практические правила и закономерности для успешной интеграции внешних провайдеров моделей (OpenRouter, SiliconFlow, HuggingFace) в Continue IDE."
scope: "Конфигурация моделей в config.yaml, устранение ошибок 401/404, Single Source of Truth для AI-конфигов."
tags: [#integrations, #llm, #continue, #ssot]
priority: high
created_at: 2026-01-30
updated_at: 2026-01-30
dependencies: ["process-continue-config-ssot", "architecture-ssot"]
---

# Подключение LLM провайдеров к Continue

## Scope
Этот навык документирует проверенные паттерны подключения различных LLM API к Continue IDE на основе реального опыта отладки OpenRouter и SiliconFlow.

## Key Rules

### 1. Общий принцип: OpenAI-совместимый провайдер
**Всегда используйте `provider: openai` для внешних API**, даже если провайдер называется по-другому (OpenRouter, SiliconFlow и т.д.). Continue имеет наиболее стабильную поддержку именно OpenAI-формата.

```yaml
# ✅ ПРАВИЛЬНО
- name: or-deepseek-r1
  provider: openai
  model: deepseek/deepseek-r1
  apiKey: sk-or-...
  apiBase: https://openrouter.ai/api/v1

# ❌ НЕПРАВИЛЬНО (вызовет ошибки парсинга)
- name: or-deepseek-r1
  provider: openrouter
  model: deepseek/deepseek-r1:free
```

### 2. Суффиксы `:free` в именах моделей (НЕПОСЛЕДОВАТЕЛЬНОЕ ПОВЕДЕНИЕ)
**Правило зависит от конкретной модели на OpenRouter:**

#### Модели БЕЗ суффикса `:free` (работают):
- `deepseek/deepseek-r1`
- `deepseek/deepseek-chat`
- `meta-llama/llama-3.3-70b-instruct`

#### Модели С БАЗОВЫМ именем без `-instruct` (работают):
- `meta-llama/llama-3.1-405b` (НЕ `llama-3.1-405b-instruct`)
- `microsoft/phi-3-medium-128k-instruct` (оставляем как есть, `-instruct` часть базового имени)

**Закономерность:** OpenRouter имеет **непоследовательное именование**. Для некоторых моделей (DeepSeek) суффикс `:free` вызывает 404, для других (Llama 405B) нужно убрать `-instruct`. 

**Стратегия отладки:**
1. Попробовать базовое имя без суффиксов.
2. Если 404 — убрать `-instruct` из имени.
3. Проверить точное имя модели на странице OpenRouter (URL модели = правильный ID).

### 3. Формат `apiBase`
**Всегда указывайте полный базовый URL с `/v1` или `/api/v1`**, если это требуется провайдером.

| Провайдер | apiBase |
|-----------|---------|
| OpenRouter | `https://openrouter.ai/api/v1` |
| SiliconFlow | `https://api.siliconflow.cn/v1` |
| Groq | `https://api.groq.com/openai/v1` |
| Ollama (локальный) | `http://localhost:11434` |
| HuggingFace Inference | `https://api-inference.huggingface.co/models` |

### 4. Хранение API ключей
**Используйте переменные окружения для секретов**, но в `config.yaml` указывайте ключи напрямую для Continue (он не поддерживает `${ENV_VAR}` синтаксис).

**Правильная схема:**
1. Храните ключи в `MBB/.env` (для Docker и n8n).
2. Дублируйте их в `config.yaml` явно (Continue читает только этот файл).

```yaml
# В config.yaml (Continue требует явных значений)
apiKey: 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
```

### 5. Роли моделей (roles)
**Указывайте роли в зависимости от возможностей модели:**
- `chat` — базовая роль для диалога (поддерживают все).
- `edit` — для моделей, способных редактировать код (DeepSeek V3, Qwen, Llama 3+).
- `apply` — для автоматического применения изменений (только самые мощные, например Mistral Small, Ollama локально).

```yaml
roles:
  - chat          # Минимум для всех
  - edit          # Если модель хороша в коде
  - apply         # Только для флагманов
```

### 6. Удаление config.ts при проблемах
**Если модели внезапно исчезают из UI**, причина в автогенерации `config.ts`:
- Continue иногда создает пустой `config.ts`, который переопределяет `config.yaml`.
- **Решение**: Удалить `config.ts` из обеих локаций (`C:\Users\<USER>\.continue` и OneDrive SSOT).

```bash
# PowerShell команда для очистки
Remove-Item "$env:USERPROFILE\.continue\config.ts" -ErrorAction SilentlyContinue
```

### 7. SSOT для Continue конфигов
**Единственный источник правды для конфигов Continue:**
- `D:\Clouds\AO\OneDrive\AI\.continue\config.yaml`
- `C:\Users\AO\.continue` — это junction (символическая ссылка) на OneDrive.

**При переносе на другой ПК:**
1. Убедитесь, что junction настроен корректно.
2. Проверьте отсутствие `config.ts` и `config.json`.
3. Только `config.yaml` должен существовать.

## Типичные ошибки и решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `404 No endpoints found` | Неправильное имя модели | 1) Убрать `:free`. 2) Убрать `-instruct` (для Llama 405B). 3) Проверить URL на OpenRouter |
| `401 Unauthorized` (SiliconFlow) | API ключ не активирован или бонусы истекли | Проверить баланс в личном кабинете |
| `401 Unauthorized` (Mistral) | Кредиты закончились | Пополнить баланс или переключиться на бесплатные модели |
| `413 Request too large` (Groq) | Превышен лимит токенов в минуту (TPM) | Использовать более крупную модель (70B вместо 8B) или подождать 1 минуту |
| `Invalid apiBase` | Неполный URL без `/v1` | Добавить `/api/v1` или `/v1` к URL |
| Модели исчезли из UI | Появился `config.ts` | Удалить `config.ts`, оставить только `config.yaml` |
| `Error loading Local Config` | YAML синтаксис нарушен (отступы) | Проверить отступы (только пробелы, 2 пробела на уровень) |
| Модель долго не отвечает (таймаут) | 1) Кредиты закончились. 2) Провайдер перегружен | Проверить баланс или переключиться на другую модель |

## Проверенные бесплатные провайдеры

### OpenRouter (бесплатно до 50 req/day)
✅ **Работающие (проверено 2026-01-30):**
- `deepseek/deepseek-r1` — мощная модель с reasoning
- `deepseek/deepseek-chat` — быстрая для чата и кода
- `meta-llama/llama-3.3-70b-instruct` — сбалансированная
- `meta-llama/llama-3.1-405b` — самая умная (без `-instruct`!)
- `microsoft/phi-3-medium-128k-instruct` — легковесная

### SiliconFlow (бесплатно с бонусами при регистрации)
⚠️ **Требует активного баланса бонусов:**
- `deepseek-ai/DeepSeek-V3` — флагман для кода
- `Qwen/Qwen2.5-7B-Instruct` — быстрая, хорошо понимает русский
- `THUDM/glm-4-9b-chat` — альтернатива от Zhipu AI

**Важно:** После регистрации SiliconFlow дает приветственные токены, но они могут быстро израсходоваться. При ошибке 401 проверьте баланс в личном кабинете.

### Groq (бесплатно, САМЫЙ БЫСТРЫЙ)
🚀 **Рекомендуется как основной бесплатный провайдер**
- Не требует привязки карты
- Скорость: до 840 токенов/сек (в 10+ раз быстрее OpenRouter)
- Консоль: [console.groq.com](https://console.groq.com)
- **Лимиты бесплатного тарифа (On-Demand):**
  - `llama-3.1-8b-instant`: 6,000 TPM (слишком мало для Continue с агент-режимом)
  - `llama-3.3-70b-versatile`: 30,000 TPM ✅ (достаточно для большинства задач)
  - `qwen-32b`: данные не указаны, но обычно выше 8B моделей
- **Рекомендуемые модели для Continue:**
  - `llama-3.3-70b-versatile` — универсальная модель (394 tok/s)
  - `qwen-32b` — отличное понимание кода
  - ⚠️ `llama-3.1-8b-instant` — НЕ рекомендуется (лимит 6k TPM, Continue отправляет ~15k токенов в первом запросе)

### Hugging Face (бесплатно Serverless API, НО ОЧЕНЬ ОГРАНИЧЕНО)
⚠️ **Только $0.10/месяц (~100-200 запросов)** — не рекомендуется для активного использования
- Любые модели с тегом "Inference API" в HF Hub
- Пример: `mistralai/Mistral-7B-Instruct-v0.2`

## Шаблон добавления новой модели

```yaml
- name: <prefix>-<model-short-name>
  provider: openai
  model: <organization>/<model-full-name>
  apiKey: <your-api-key>
  apiBase: https://<provider-domain>/v1
  roles:
    - chat
    - edit  # optional
```

## Workflow добавления нового провайдера

1. Получить API ключ в личном кабинете провайдера.
2. Сохранить ключ в `MBB/.env` (для истории).
3. Добавить модель в `config.yaml` (OneDrive SSOT) по шаблону выше.
4. **КРИТИЧНО**: Использовать `provider: openai`, даже если провайдер не OpenAI.
5. Убрать суффиксы типа `:free` из имени модели.
6. Перезагрузить Continue (Reload).
7. Протестировать запрос к новой модели.

## References
- `skills-mbb/skills/process/process-continue-config-ssot.md`
- `skills-mbb/skills/architecture/architecture-ssot.md`
- OpenRouter Docs: https://openrouter.ai/docs
- SiliconFlow Docs: https://docs.siliconflow.com/

## Metadata
- created_at: 2026-01-30
- priority: high
- verified: true (протестировано на OpenRouter и SiliconFlow)
